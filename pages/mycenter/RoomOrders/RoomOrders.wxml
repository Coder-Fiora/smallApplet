<TopBar tabs="{{true}}"></TopBar>
<view>
    <van-tabs active="{{ active }}" bind:change="onChange">
        <van-tab title="{{item.title}}" wx:for="{{tabs}}" wx:key="{{index}}">
            <roomOrderCard orderList="{{orderList}}"></roomOrderCard>
        </van-tab>
    </van-tabs>
</view>